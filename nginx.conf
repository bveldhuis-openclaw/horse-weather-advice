server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  location / {
    try_files $uri $uri/ /index.html;
  }
  location ~* \.(?:png|jpg|jpeg|gif|css|js|json)$ {
    expires 7d;
    add_header Cache-Control "public";
  }
}
